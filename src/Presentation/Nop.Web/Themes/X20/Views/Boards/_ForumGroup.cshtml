@model ForumGroupModel
@using Nop.Web.Models.Boards;
<div class="forums-table-section forum-group">
    <div class="forums-table-section-title">
        @Html.RouteLink(Model.Name, "ForumGroupSlug", new { id = Model.Id, slug = Model.SeName })
    </div>
    <div class="forums-table-section-body">
        <table class="footable">
            <thead>
                <tr>
                    <th>
                    </th>
                    <th class="forum-name" colspan="2">
                        @T("Forum.Forum")
                    </th>
                    <th class="topics">
                        @T("Forum.Topics")
                    </th>
                    <th data-hide="phone" class="posts">
                        @T("Forum.Posts")
                    </th>
                    <th data-hide="phone" class="last-post">
                        @T("Forum.LatestPost")
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var forum in Model.Forums)
                {
                    <tr>
                        <td>
                        </td>
                        <td class="image">
                            <div></div>
                        </td>
                        <td class="forum-name">
                            <div class="forum-title">
                                @Html.RouteLink(forum.Name, "ForumSlug", new { id = forum.Id, slug = forum.SeName })
                            </div>
                            <div class="forum-description">@forum.Description
                            </div>
                        </td>
                        <td class="topics">@forum.NumTopics
                        </td>
                        <td class="posts">@forum.NumPosts
                        </td>
                        <td class="last-post">
                            @Html.Action("LastPost", new { forumPostId = forum.LastPostId, showTopic = true })
                        </td>
                    </tr>            
                }
            </tbody>
        </table>
    </div>
</div>
